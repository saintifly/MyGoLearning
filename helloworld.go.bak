package main//声明包--没有分号，编译器级别自动添加分号，表明该文件里的函数属于哪一个包

import (//导入包--程序中需要使用哪些包就需要事先导入。import导入的包可以用小括号括起来，小括号后面没有分号
    "fmt"//常用于输入输出
    //多个包之间直接换行，不需用逗号或者分号
)

func main(){//所有的可执行程序都需要一个main函数，main函数没有返回类型，而且处于main包里面
    fmt.Println("Hello Golang")//没有分号
}